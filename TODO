+ Add options to eguebfs, like:
  -d to daemonize
  -g to create the window
  -v to be verbose? or use the debug?
  -h to get the help
  -v to get the version
+ Change egueb node map named to not be live
+ Support MT on egueb
